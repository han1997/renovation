# Android 全 App UI、功能与逻辑完善

## 已确认决策
覆盖五 Tab、首次设置、报价及需求规划。原生 Material 3 精修，保留动态色/深色/五 Tab。补齐报价完整能力。房屋总预算为独立上限，应用报价只覆盖确认过的分类计划，支出不动。施工方报价与自购分列，合计比较预算。需求推荐按空间类型，预览后应用，不覆盖手工结果。全部主会话实施，禁止子代理。

## 要求
- 精确金额解析与格式化；真实写入反馈；加载/就绪/错误/重试。
- 备份 v2 整数分，覆盖任务模板/完成/日期、报价、规划及现有全部用户数据；兼容 v1 缺失模块保留；校验、预览、事务恢复；重置恢复默认模板，不删除外部备份。
- 任务日期编辑/清除、首页跨日与阶段定位、恢复默认保留自定义任务；预算三口径；指南搜索和验收联动；我的分组；首次设置原子提交。
- 报价四步与局改独立输入；吊顶/工艺/面积单价覆盖/空间附加/主材矩阵；方案列表、保存、加载、改名、另存、删除、未保存离开确认；完整文本/图片清单。
- 分类应用预览：费用大项映射，按目标汇总覆盖，未知分类必须选择，事务执行，幂等，总上限和支出不变。
- 需求全目录搜索、同类型多房间、引用清理、未分配独立组、串行自动保存与重试。
- 统一间距/排版、紧凑列表、无效点击修复、窄屏/键盘/安全区；子流程隐藏 Tab；返回恢复状态。
- 长图实际行高与分段渲染，Android 10+ 相册保存，旧系统分享。

## 验收
- [x] 数据全量往返、v1 兼容、错误原子性、正确重置。
- [x] 金额精度/Locale/非法值、预算映射幂等及独立上限。
- [x] 三模式完整配置、方案持久化、全部清单导出。
- [x] 规划搜索/多房间/推荐/引用/自动保存。
- [x] 日期跨日、指南联动、首次设置、失败反馈、导航恢复。
- [x] testDebugUnitTest、lintDebug、assembleDebug；Compose 测试补齐。
- [ ] API 24/31/34/35 窄屏/大字体/深色/动态色/edge-to-edge 设备验收（当前无设备）。

## 边界
保留 Room v3/既有 ID/离线目录公式；不做 Web、账号、云同步、多房屋、后端及在线价格。报价显式保存，规划自动保存；开工日期变化不重排任务。完成后更新规范/README，先提提交计划，不自动提交推送。

## 进度
实现与自动化验收已完成；API 36 独立包真机 5 项通过，旧 API 矩阵及人工验收待补。尚未提交。详见 research/verification.md。
